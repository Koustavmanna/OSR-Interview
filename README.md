# OSR Model Builder

Run locally:

```
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Get your Groq API key: https://console.groq.com/keys


```
add .env file in folder
and paste the keys shared over email
```
